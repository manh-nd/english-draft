# Handoff

Use the `handoff` skill (`.agents/skills/handoff/SKILL.md`) to compact the current conversation into a structured handoff document that another agent can pick up. Use at the end of a long session or when switching context.
