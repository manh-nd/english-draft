# Debugging

Use the `diagnosing-bugs` skill (`.agents/skills/diagnosing-bugs/SKILL.md`) for a structured diagnosis loop on hard bugs and performance regressions. Trigger when the user says "diagnose", "debug this", or reports something broken, throwing, failing, or slow.
